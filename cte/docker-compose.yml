version: '3.3'
services:
  mysql:
    image: mysql:8.0.36
    environment:
      MYSQL_ROOT_PASSWORD: 'root'
    ports:
      - 3306:3306
    volumes:
      - ./COMPANY.sql:/docker-entrypoint-initdb.d/COMPANY.sql
